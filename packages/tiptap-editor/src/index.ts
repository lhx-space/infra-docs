/**
 * @lhx-kit/tiptap-editor — @lhx-kit/tiptap-editor package (scaffolded by lhx-cli add package).
 *
 * Replace the starter export below with the real API surface for this package.
 * Remember to update README.md with usage examples before publishing.
 */

export const tiptapEditorVersion = '0.0.0';

/**
 * Example helper. Delete once you add your real implementation.
 */
export function helloTiptapEditor(who = 'world'): string {
  return `Hello, ${who}! (from @lhx-kit/tiptap-editor)`;
}
