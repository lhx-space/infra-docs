# .

> Generated by `lhx-cli create --template=business-mono` on 2026.

Full-stack pnpm monorepo with workspace orchestration.

## Layout

```
./
├── apps/
│   ├── api/          — Express + TypeScript API service
│   └── web/          — React + Vite MPA
├── packages/
│   ├── types/        — shared TypeScript types
│   └── utils/        — shared utility functions
└── pnpm-workspace.yaml
```

## Quick start

```bash
pnpm install
cp apps/api/.env.example apps/api/.env
pnpm dev            # starts all apps in parallel
```

| URL | App |
| --- | --- |
| http://localhost:5173 | web (Vite dev server) |
| http://localhost:3000 | api |
| http://localhost:3000/healthz | API health |

## Scripts

```bash
pnpm build       # pnpm workspace build all apps + packages
pnpm dev         # parallel dev mode
pnpm typecheck   # tsc across workspace
pnpm lint        # biome check
```

## Resources

- https://github.com/juwenzhang/lhx-kit/issues
- https://juwenzhang.github.io/lhx-kit/
